services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      HOST: "0.0.0.0"
      ADMIN_USER: "admin"
      ADMIN_PASS: "admin123!"
    restart: unless-stopped
